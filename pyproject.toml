[project]
name = "blog-app"
version = "0.1.0"
description = "Production-ready Django backend built while learning Django from scratch."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.8",
    "pillow>=11.0.0",
]
